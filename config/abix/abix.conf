# Abix Layer Configuration
# This file extends HyDE with custom Abix features

# Custom environment variables
$ABIX_CONFIG = $XDG_CONFIG_HOME/abix

# Example: Custom scripts path (uncomment to enable)
# exec-once = $ABIX_CONFIG/scripts/abix-start.sh

# Custom keybinds can be added here
# See ~/.config/hypr/keybindings.conf for HyDE defaults